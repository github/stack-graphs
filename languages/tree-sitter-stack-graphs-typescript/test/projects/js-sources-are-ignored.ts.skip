/* --- path: tsconfig.json --- */
{
}

/* --- path: lib/index.js --- */

/* --- path: src/foo.ts --- */
export const bar = 42;

/* --- path: src/index.ts --- */
import { bar } from "./foo";
//       ^ defined: 8
